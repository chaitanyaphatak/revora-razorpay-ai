import { StatusPill, humanize } from "@/components/recoverai/StatusPill";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { trpc } from "@/lib/trpc";
import { Bot, CircleAlert, ExternalLink, Loader2, ShieldCheck, Sparkles } from "lucide-react";
import { useState } from "react";
import { Link } from "wouter";

export default function AIBrief() {
  const [paymentId, setPaymentId] = useState("P00272");
  const explanation = trpc.recovery.geminiExplanation.useMutation();
  const requestExplanation = () => explanation.mutate({ paymentId: paymentId.trim().toUpperCase() });
  const output = explanation.data;

  return <div className="mx-auto max-w-7xl space-y-5 pb-10"><section className="border-b border-slate-200 pb-5"><div className="max-w-2xl"><p className="rr-eyebrow">Gemini business brief</p><h1 className="mt-1 text-3xl font-semibold tracking-[-.05em] text-slate-950">AI explanation, <span className="text-violet-600">bounded by policy.</span></h1><p className="mt-2 text-sm leading-6 text-slate-500">Ask Gemini to translate the already-computed model and deterministic policy context into a concise business brief. Gemini cannot select a new action, override a safety gate, or execute a payment.</p></div></section>
    <div className="grid gap-5 xl:grid-cols-[.75fr_1.25fr]"><section className="rr-surface p-5 sm:p-6"><div className="flex items-center justify-between"><div><p className="rr-eyebrow">Input</p><h2 className="mt-1 text-xl font-semibold tracking-[-.04em]">Select a payment</h2></div><Bot className="h-5 w-5 text-violet-600" /></div><label className="mt-5 block"><span className="text-xs font-bold text-slate-600">Synthetic payment ID</span><Input value={paymentId} onChange={event => setPaymentId(event.target.value.toUpperCase())} className="mt-2 h-11 rounded-lg border-slate-200 bg-white font-mono" /></label><Button onClick={requestExplanation} disabled={explanation.isPending || !paymentId.trim()} className="mt-4 w-full rounded-lg bg-violet-600 text-white hover:bg-violet-700">{explanation.isPending ? <><Loader2 className="mr-2 h-4 w-4 animate-spin" />Generating brief…</> : <><Sparkles className="mr-2 h-4 w-4" />Generate AI brief</>}</Button>{paymentId.trim() ? <Link href={`/payments/${paymentId.trim().toUpperCase()}`} className="mt-4 inline-flex text-xs font-bold text-teal-700 hover:text-teal-900">Inspect payment detail <ExternalLink className="ml-1 h-3.5 w-3.5" /></Link> : null}</section>
      <section className="rr-surface p-5 sm:p-6"><div className="flex items-center justify-between"><div><p className="rr-eyebrow">Structured result</p><h2 className="mt-1 text-xl font-semibold tracking-[-.04em]">Business-readable recovery context</h2></div><ShieldCheck className="h-5 w-5 text-teal-600" /></div>{explanation.error ? <div className="mt-5 rounded-xl border border-rose-100 bg-rose-50 p-5"><CircleAlert className="h-6 w-6 text-rose-500" /><p className="mt-2 text-sm font-bold text-rose-900">Gemini could not generate a brief.</p><p className="mt-1 text-sm text-rose-800">{explanation.error.message}</p></div> : output ? <div className="mt-5 space-y-4"><div className="rounded-xl border border-violet-100 bg-violet-50/60 p-4"><p className="text-[10px] font-bold uppercase tracking-wider text-violet-700">Diagnosis summary</p><p className="mt-2 text-base font-semibold text-violet-950">{output.diagnosisSummary}</p><p className="mt-3 text-sm leading-6 text-slate-700">{output.businessExplanation}</p></div><div className="rounded-xl border border-slate-200 bg-slate-50/80 p-4"><p className="text-[10px] font-bold uppercase tracking-wider text-slate-500">Risk note</p><p className="mt-2 text-sm leading-6 text-slate-800">{output.riskNote}</p></div><div className="rounded-xl border border-slate-100 bg-white p-4"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="text-[10px] font-bold uppercase tracking-wider text-slate-500">Deterministic recommendation</p><p className="mt-1 text-sm font-semibold text-slate-800">{humanize(output.deterministicRecommendation.action)}</p></div><StatusPill status={output.deterministicRecommendation.policyResult} /></div><p className="mt-3 text-sm leading-6 text-slate-600">{output.deterministicRecommendation.reason}</p><p className="mt-3 inline-flex rounded-full bg-slate-100 px-2 py-1 text-[10px] font-bold text-slate-500">{output.deterministicRecommendation.ruleCode}</p></div><p className="text-center text-[11px] font-semibold text-slate-400">Generated by {output.model}. The deterministic policy above remains the authority.</p></div> : <div className="mt-5 flex min-h-72 items-center justify-center rounded-xl border border-dashed border-slate-200 bg-slate-50 p-8 text-center"><div><Bot className="mx-auto h-8 w-8 text-violet-300" /><p className="mt-3 text-sm font-bold text-slate-600">Generate a bounded business brief</p><p className="mt-1 max-w-sm text-sm leading-6 text-slate-500">The result will explain the deterministic recovery decision in clear business language, without changing it.</p></div></div>}</section></div></div>;
}
