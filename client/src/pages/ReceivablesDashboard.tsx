import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { AnimatedMetric, EnvironmentStrip } from "@/components/recoverai/DesignPrimitives";
import { Input } from "@/components/ui/input";
import { Skeleton } from "@/components/ui/skeleton";
import { trpc } from "@/lib/trpc";
import { AlertTriangle, ArrowRight, BadgeIndianRupee, CalendarClock, CheckCircle2, CircleAlert, FileText, HandCoins, Search, ShieldCheck, Sparkles } from "lucide-react";
import { useMemo, useState } from "react";
import { Link } from "wouter";

const money = (amount: number) => new Intl.NumberFormat("en-IN", { style: "currency", currency: "INR", maximumFractionDigits: 0 }).format(amount);
const date = (value: string) => new Intl.DateTimeFormat("en-IN", { dateStyle: "medium" }).format(new Date(`${value.slice(0, 10)}T00:00:00`));
const label = (value: string | null | undefined) => (value ?? "unknown").replace(/_/g, " ").replace(/\b\w/g, letter => letter.toUpperCase());
const riskStyle = (risk: string) => risk === "high" ? "border-rose-100 bg-rose-50 text-rose-700" : risk === "medium" ? "border-amber-100 bg-amber-50 text-amber-800" : "border-teal-100 bg-teal-50 text-teal-700";

function MetricCard({ title, value, format, detail, icon: Icon, tone }: { title: string; value: number; format: (value: number) => string; detail: string; icon: typeof FileText; tone: "slate" | "teal" | "amber" | "rose" | "violet" }) {
  const colors = { slate: "bg-slate-100 text-slate-700", teal: "bg-teal-50 text-teal-700", amber: "bg-amber-50 text-amber-700", rose: "bg-rose-50 text-rose-700", violet: "bg-violet-50 text-violet-700" };
  return <article className="rr-kpi p-4 sm:p-5"><div className="flex items-start justify-between gap-4"><div><p className="rr-eyebrow">{title}</p><p className="mt-2 text-2xl font-semibold tracking-[-.05em] text-slate-950"><AnimatedMetric value={value} format={format} /></p><p className="mt-1 text-xs leading-5 text-slate-500">{detail}</p></div><span className={`flex h-9 w-9 shrink-0 items-center justify-center rounded-xl ${colors[tone]}`}><Icon className="h-4 w-4" /></span></div></article>;
}

function SetupRequired({ message }: { message?: string }) {
  return <section className="rounded-2xl border border-amber-200 bg-amber-50 p-5 sm:p-6"><div className="flex gap-3"><AlertTriangle className="mt-0.5 h-5 w-5 shrink-0 text-amber-700" /><div><p className="font-semibold text-amber-950">Invoice data setup is required</p><p className="mt-1 text-sm leading-6 text-amber-900">{message ?? "The invoices table is not available in the configured Supabase project."}</p><p className="mt-3 text-xs font-semibold text-amber-800">Review `database/README.md` and `database/RECEIVABLES_SETUP.md`, then manually apply `database/04_add_invoice_receivables.sql` and import approved invoice source records. No sample invoices are created automatically.</p></div></div></section>;
}

export default function ReceivablesDashboard() {
  const dashboard = trpc.invoices.dashboard.useQuery();
  const [search, setSearch] = useState("");
  const [status, setStatus] = useState("");
  const [risk, setRisk] = useState("");
  const [overdueOnly, setOverdueOnly] = useState(false);
  const [page, setPage] = useState(1);
  const filters = useMemo(() => ({ page, pageSize: 10, search: search || undefined, status: (status || undefined) as "draft" | "open" | "partially_paid" | "paid" | "written_off" | "disputed" | "cancelled" | "unknown" | undefined, risk: (risk || undefined) as "low" | "medium" | "high" | undefined, overdueOnly }), [page, search, status, risk, overdueOnly]);
  const invoices = trpc.invoices.list.useQuery(filters);
  const metric = dashboard.data?.metrics;
  const totalPages = Math.max(1, Math.ceil((invoices.data?.total ?? 0) / 10));
  const isDemoWorkspace = Boolean(dashboard.data?.recentInvoices.some(invoice => invoice.isSimulationDemo));
  const updateFilter = (callback: () => void) => { setPage(1); callback(); };

  return <div className="rr-page mx-auto max-w-7xl space-y-5 pb-10">{isDemoWorkspace ? <EnvironmentStrip><span><strong>Simulation demonstration workspace.</strong> Records beginning with <code>DEMO-</code> are clearly labelled test scenarios for reviewing invoice policy, Promise-to-Pay, audit, and recovery flows. They are not imported customer source records.</span></EnvironmentStrip> : null}
    <section className="overflow-hidden rounded-2xl border border-teal-100 bg-gradient-to-br from-white via-white to-teal-50 p-5 shadow-sm sm:p-7"><div className="flex flex-col gap-5 xl:flex-row xl:items-end xl:justify-between"><div className="max-w-2xl"><div className="flex flex-wrap items-center gap-2"><p className="rr-eyebrow">B2B receivables recovery</p><Badge variant="outline" className="border-amber-200 bg-amber-50 text-[10px] font-bold text-amber-800"><ShieldCheck className="mr-1 h-3 w-3" />Simulation only</Badge></div><h1 className="mt-2 text-3xl font-semibold tracking-[-.05em] text-slate-950 sm:text-4xl">Turn aged receivables into a <span className="text-teal-600">clear next review.</span></h1><p className="mt-3 text-sm leading-6 text-slate-600">Monitor invoice exposure, receive a deterministic recovery recommendation, and record bounded simulations with a complete audit trail. No message, payment, or source invoice change occurs here.</p></div><Link href="/invoices/promises"><Button className="rounded-xl bg-slate-900 px-4 text-white hover:bg-slate-800"><CalendarClock className="mr-2 h-4 w-4" />Promise-to-Pay tracker</Button></Link></div></section>
    {dashboard.isLoading ? <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-5">{Array.from({ length: 5 }, (_, index) => <Skeleton key={index} className="rr-skeleton h-32 rounded-2xl" />)}</div> : <div className="rr-stagger grid gap-4 sm:grid-cols-2 xl:grid-cols-5"><MetricCard title="Total invoices" value={metric?.totalInvoices ?? 0} format={value => Math.round(value).toLocaleString("en-IN")} detail="Approved invoice source records" icon={FileText} tone="slate" /><MetricCard title="Outstanding" value={metric?.outstandingAmount ?? 0} format={money} detail="After simulated recoveries" icon={BadgeIndianRupee} tone="teal" /><MetricCard title="Overdue" value={metric?.overdueAmount ?? 0} format={money} detail={`${metric?.overdueCount ?? 0} invoices past due`} icon={CalendarClock} tone="amber" /><MetricCard title="At risk" value={metric?.atRiskAmount ?? 0} format={money} detail={`${metric?.atRiskCount ?? 0} high-risk or missed promises`} icon={AlertTriangle} tone="rose" /><MetricCard title="Recovered" value={metric?.recoveredAmount ?? 0} format={money} detail="Simulation outcomes included in revenue" icon={CheckCircle2} tone="violet" /></div>}
    {dashboard.data?.setupRequired ? <SetupRequired message={"message" in dashboard.data ? dashboard.data.message : undefined} /> : null}
    <section className="rr-surface overflow-hidden"><div className="flex flex-col gap-4 border-b border-slate-100 p-5 sm:p-6 lg:flex-row lg:items-end lg:justify-between"><div><p className="rr-eyebrow">Invoice explorer</p><h2 className="mt-1 text-xl font-semibold tracking-[-.04em] text-slate-950">Prioritise the receivables queue</h2><p className="mt-1 text-sm text-slate-500">Search and filter the approved B2B invoice source.</p></div><div className="grid gap-2 sm:grid-cols-2 lg:flex"><div className="relative sm:col-span-2"><Search className="absolute left-3 top-3 h-4 w-4 text-slate-400" /><Input value={search} onChange={event => updateFilter(() => setSearch(event.target.value))} placeholder="Invoice, customer, reference" className="h-10 w-full rounded-lg border-slate-200 bg-slate-50 pl-9 text-sm lg:w-60" /></div><select value={status} onChange={event => updateFilter(() => setStatus(event.target.value))} className="h-10 rounded-lg border border-slate-200 bg-white px-3 text-sm text-slate-700"><option value="">All statuses</option><option value="open">Open</option><option value="partially_paid">Partially paid</option><option value="disputed">Disputed</option><option value="paid">Paid</option></select><select value={risk} onChange={event => updateFilter(() => setRisk(event.target.value))} className="h-10 rounded-lg border border-slate-200 bg-white px-3 text-sm text-slate-700"><option value="">All recovery risks</option><option value="high">High risk</option><option value="medium">Medium risk</option><option value="low">Low risk</option></select><label className="flex h-10 items-center gap-2 rounded-lg border border-slate-200 bg-white px-3 text-xs font-semibold text-slate-600"><input checked={overdueOnly} onChange={event => updateFilter(() => setOverdueOnly(event.target.checked))} type="checkbox" className="accent-teal-600" />Overdue only</label></div></div>
      {invoices.isLoading ? <div className="space-y-3 p-5">{Array.from({ length: 5 }, (_, index) => <Skeleton key={index} className="rr-skeleton h-14 rounded-lg" />)}</div> : invoices.error ? <div className="p-6 text-sm text-rose-700">{invoices.error.message}</div> : !invoices.data?.invoices.length ? <div className="p-10 text-center"><HandCoins className="rr-empty-icon mx-auto h-8 w-8 text-teal-300" /><p className="mt-3 text-sm font-bold text-slate-700">No invoices match this view</p><p className="mt-1 text-sm text-slate-500">{invoices.data?.setupRequired ? "Apply the approved receivables migration and connect invoice source records to begin." : "Try changing the search or filters."}</p></div> : <><div className="overflow-x-auto"><table className="w-full min-w-[850px] text-left"><thead className="bg-slate-50 text-[10px] font-bold uppercase tracking-wider text-slate-500"><tr><th className="px-5 py-3 sm:px-6">Invoice</th><th className="px-5 py-3">Customer</th><th className="px-5 py-3 text-right">Outstanding</th><th className="px-5 py-3">Due date</th><th className="px-5 py-3">Status</th><th className="px-5 py-3">Recovery risk</th><th className="px-5 py-3">AI recommendation</th><th className="px-5 py-3" /></tr></thead><tbody className="divide-y divide-slate-100">{invoices.data.invoices.map(invoice => <tr key={invoice.id} className="rr-table-row"><td className="px-5 py-4 sm:px-6"><p className="font-mono text-sm font-bold text-slate-800">{invoice.id}</p><p className="mt-1 text-xs text-slate-400">{invoice.daysOverdue ? `${invoice.daysOverdue}d overdue` : "Current"}</p></td><td className="px-5 py-4"><p className="text-sm font-semibold text-slate-700">{invoice.customerName ?? invoice.customerId}</p><p className="mt-1 text-xs text-slate-400">{invoice.customerId}</p></td><td className="px-5 py-4 text-right text-sm font-semibold text-slate-800">{money(invoice.outstandingAmount)}</td><td className="px-5 py-4 text-sm text-slate-600">{date(invoice.dueDate)}</td><td className="px-5 py-4"><Badge variant="outline" className="border-slate-200 bg-white text-xs text-slate-600">{label(invoice.status)}</Badge></td><td className="px-5 py-4"><Badge variant="outline" className={`text-xs ${riskStyle(invoice.recoveryRisk)}`}>{label(invoice.recoveryRisk)} · {Math.round(invoice.recoveryProbability * 100)}%</Badge>{invoice.activePromise?.isMissed ? <p className="mt-1 text-[10px] font-bold text-rose-600">Promise missed</p> : null}</td><td className="px-5 py-4 text-sm font-semibold text-slate-700">{label(invoice.recommendedAction)}</td><td className="px-5 py-4"><Link href={`/invoices/${invoice.id}`}><Button variant="ghost" size="sm" className="rr-row-action rounded-lg text-teal-700 hover:bg-teal-50 hover:text-teal-800">Review <ArrowRight className="ml-1 h-3.5 w-3.5" /></Button></Link></td></tr>)}</tbody></table></div><div className="flex flex-wrap items-center justify-between gap-3 border-t border-slate-100 px-5 py-4 sm:px-6"><p className="text-xs text-slate-500">{invoices.data.total} invoice{invoices.data.total === 1 ? "" : "s"} found</p><div className="flex items-center gap-2"><Button variant="outline" size="sm" disabled={page <= 1} onClick={() => setPage(current => current - 1)} className="rounded-lg">Previous</Button><span className="text-xs font-semibold text-slate-600">Page {page} of {totalPages}</span><Button variant="outline" size="sm" disabled={page >= totalPages} onClick={() => setPage(current => current + 1)} className="rounded-lg">Next</Button></div></div></>}
    </section>
    <section className="grid gap-4 lg:grid-cols-3"><article className="rr-surface p-5"><p className="rr-eyebrow">Recovery policy</p><p className="mt-2 text-sm font-semibold text-slate-800">Rules decide what is eligible</p><p className="mt-2 text-xs leading-5 text-slate-500">Timing, value, dispute state, recovery probability, and missed promises are checked before every simulation.</p></article><article className="rr-surface p-5"><p className="rr-eyebrow">Gemini analysis</p><p className="mt-2 text-sm font-semibold text-slate-800">Explanation, not authority</p><p className="mt-2 text-xs leading-5 text-slate-500">Gemini explains limited overdue-invoice context. It cannot change a policy result, record a promise, or contact a customer.</p></article><article className="rr-surface p-5"><p className="rr-eyebrow">Audit trail</p><p className="mt-2 text-sm font-semibold text-slate-800">Every simulation stays traceable</p><p className="mt-2 text-xs leading-5 text-slate-500">Policy decisions, simulated actions, Promise-to-Pay records, and outcomes have isolated invoice audit entries.</p></article></section>
  </div>;
}

export { SetupRequired, date, label, money, riskStyle };
